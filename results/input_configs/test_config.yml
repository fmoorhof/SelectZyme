# Doubts about:
# - the synthax for configuration? check out: https://yamline.com/tutorial/
# - how query terms match UniProt (query_terms)? https://www.uniprot.org/help/text-search
# - additional return fields (df_coi): https://www.uniprot.org/help/query-fields

project: 
  name: "_test_config"
  port: 8050
  use_DB: False  # use qdrant vector DB to save embeddings for project:name
  preprocessing: False

  # Specify data you want to analyze
  data:
    query_terms:  # create in ascending manner: smallest return sets first
      - "xref:brenda-1.13.11.1"  # external reference (xref) to BRENDA database
      # - "xref:brenda-1.13.11.1*"  # wildcard search
      # - "ec:1.13.11.1"  # EC number
      - "protein_name: catchol 1,2-dioxygenases"  # free text search
      - "catchol 1,2-dioxygenases"  # free text search
    length: "10 TO *"  # *=open end
    custom_data_location: "src/tests/head_10.tsv"  # use "" if no custom data is desired
    out_dir: "results/"
    df_coi: ["accession", "reviewed", "ec", "organism_id", "length", "xref_brenda", "xref_pdb", "sequence"]  # long inline lists also possible or write like in 'query_terms'

  plm:
    plm_model: "prott5" # [esm1b, esm2, prott5, prostt5]

  clustering:
    min_samples: 2
    min_cluster_size: 2

  dimred:
    random_state: 42
    method: "UMAP"  # [UMAP, TSNE, openTSNE, PCA]
    n_neighbors: 15  # recommended between 5-50

  plot_customizations:  # set marker size and shape for plots
    size: [6, 8, 14]  # [unknown data type, UniProt annotation, BRENDA annotation]
    shape: ["circle", "diamond", "cross"]  # [unknown data type, UniProt annotation, BRENDA annotation]
